import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ScanFaceScreen = () => {
  return (
    <View>
      <Text>ScanFaceScreen</Text>
    </View>
  );
};

export default ScanFaceScreen;

const styles = StyleSheet.create({});
