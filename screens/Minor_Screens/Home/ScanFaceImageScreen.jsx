import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ScanFaceImageScreen = () => {
  return (
    <View>
      <Text>ScanFaceImageScreen</Text>
    </View>
  );
};

export default ScanFaceImageScreen;

const styles = StyleSheet.create({});
